[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pcappuller"
dynamic = ["version"]
description = "A fast PCAP window selector, merger, trimmer, and cleaner"
readme = "README.md"
authors = [
  { name = "Kyle Versluis" }
]
license = { file = "LICENSE" }
requires-python = ">=3.10"
keywords = ["pcap", "wireshark", "network", "analysis", "packet", "capture", "forensics"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Environment :: X11 Applications",
  "Intended Audience :: System Administrators",
  "Intended Audience :: Information Technology",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: System :: Networking :: Monitoring",
  "Topic :: System :: Systems Administration",
]
dependencies = [
  "tqdm",
]

[project.urls]
Homepage = "https://github.com/ktalons/PCAPpuller"
"Bug Reports" = "https://github.com/ktalons/PCAPpuller/issues"
"Source" = "https://github.com/ktalons/PCAPpuller"
"Documentation" = "https://github.com/ktalons/PCAPpuller/blob/main/docs/Analyst-Guide.md"

[project.optional-dependencies]
# pip install .[gui]
gui = ["FreeSimpleGUI"]
# pip install .[datetime]
datetime = ["python-dateutil"]
# pip install -e .[dev]
dev = ["pytest", "ruff", "mypy", "pre-commit"]

[project.scripts]
pcap-puller = "pcappuller.cli:main"
pcap-puller-gui = "pcappuller.gui:main"
pcap-clean = "pcappuller.clean_cli:main"

[tool.setuptools]
packages = ["pcappuller"]

[tool.setuptools.package-data]
pcappuller = ["assets/*.png"]

[tool.setuptools.dynamic]
version = { attr = "pcappuller.__version__" }

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py310"
line-length = 100
extend-exclude = ["_smoke_data", ".venv"]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true
disable_error_code = ["import-untyped"]
warn_redundant_casts = true
warn_unused_ignores = true
warn_return_any = true
no_implicit_optional = false
pretty = true
show_error_codes = true
